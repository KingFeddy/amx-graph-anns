diff --git a/faiss/impl/HNSW.cpp b/faiss/impl/HNSW.cpp
index b356da967..9e1d87506 100644
--- a/faiss/impl/HNSW.cpp
+++ b/faiss/impl/HNSW.cpp
@@ -15,6 +15,12 @@
 #include <faiss/impl/DistanceComputer.h>
 #include <faiss/impl/IDSelector.h>
 
+namespace faiss_instr {
+std::vector<HNSWHopEntry> hnsw_hop_log;
+bool hnsw_instr_enabled = false;
+thread_local int hnsw_current_qid = -1;
+}
+
 namespace faiss {
 
 /**************************************************************
@@ -544,7 +550,7 @@ int search_from_candidates(
     }
 
     int nstep = 0;
-
+    int _instr_hop = 0;
     while (candidates.size() > 0) {
         float d0 = 0;
         int v0 = candidates.pop_min(&d0);
@@ -573,6 +579,11 @@ int search_from_candidates(
             vt.set(v1);
             ndis++;
             float d = qdis(v1);
+            if (faiss_instr::hnsw_instr_enabled && level == 0) {
+                faiss_instr::hnsw_hop_log.push_back(
+                    {faiss_instr::hnsw_current_qid, _instr_hop, v1}
+                );
+            }
             if (!sel || sel->is_member(v1)) {
                 if (nres < k) {
                     faiss::maxheap_push(++nres, D, I, d, v1);
@@ -584,6 +595,7 @@ int search_from_candidates(
         }
 
         nstep++;
+        _instr_hop++;
         if (!do_dis_check && nstep > efSearch) {
             break;
         }
diff --git a/faiss/impl/HNSW.h b/faiss/impl/HNSW.h
index 6e2524ec5..554715f21 100644
--- a/faiss/impl/HNSW.h
+++ b/faiss/impl/HNSW.h
@@ -21,6 +21,23 @@
 #include <faiss/utils/Heap.h>
 #include <faiss/utils/random.h>
 
+#include <vector>
+
+namespace faiss_instr {
+
+struct HNSWHopEntry {
+    int query_id;
+    int hop;
+    int node_id;
+};
+
+extern std::vector<HNSWHopEntry> hnsw_hop_log;
+extern bool hnsw_instr_enabled;
+extern thread_local int hnsw_current_qid;
+
+}
+
+
 namespace faiss {
 
 /** Implementation of the Hierarchical Navigable Small World
